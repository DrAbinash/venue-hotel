'use client';

import HomePage from '@/app/page';

/** /my-booking — direct link to guest booking lookup. */
export default function MyBookingRoute() {
  return <HomePage initialView="my-booking" />;
}
