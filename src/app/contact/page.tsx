'use client';

import HomePage from '@/app/page';

/** /contact — direct link to the contact and enquiry page. */
export default function ContactRoute() {
  return <HomePage initialView="contact" />;
}
