'use client';

import HomePage from '@/app/page';

/** /restaurant — a shareable, bookmarkable link straight to online ordering. */
export default function RestaurantRoute() {
  return <HomePage initialView="restaurant" />;
}
